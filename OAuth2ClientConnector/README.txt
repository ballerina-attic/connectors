Description
===========
The OAUTH connector allows you to handle the authentication flow. This OAUTH connector will be used in Ballerina Client Connectors
for authentication flow. This connector allows to handle the access token in the request and to refresh the access token if get 401 responses to the actual call.
In client connector, you can persist the new token in a service level variable.
